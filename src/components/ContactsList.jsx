import React from "react";
const ContactsList = (props) => {
    console.log(props);
    return (
        <div>
            <div>ContactsList</div>
        </div>
    );
};

export default ContactsList;
